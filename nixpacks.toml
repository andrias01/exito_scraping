# nixpacks.toml
# Configura el build de Railway para soportar Playwright + Chromium

[phases.setup]
nixPkgs = [
  "chromium",
  "nss",
  "freetype",
  "harfbuzz",
  "ca-certificates",
  "glib",
  "nspr",
  "atk",
  "cups",
  "xorg.libX11",
  "xorg.libXcomposite",
  "xorg.libXdamage",
  "xorg.libXext",
  "xorg.libXfixes",
  "xorg.libXrandr",
  "xorg.libXrender",
  "xorg.libXtst",
  "libxcb",
  "pango",
  "libdrm",
  "mesa",
  "expat",
  "xorg.libxkbcommon",
  "alsa-lib",
]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "PLAYWRIGHT_BROWSERS_PATH=/ms-playwright playwright install chromium",
  "PLAYWRIGHT_BROWSERS_PATH=/ms-playwright playwright install-deps chromium",
]

# Fijar la ruta de browsers para build Y runtime
[variables]
PLAYWRIGHT_BROWSERS_PATH = "/ms-playwright"

[start]
cmd = "python run_api.py"
